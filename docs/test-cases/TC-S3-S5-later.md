# TC-S3/S4/S5 · 后续切片骨架用例（启动时细化）

> 说明：以下为骨架级用例——覆盖 PRD 要求的核心断言点，具体步骤在对应切片启动时（按当时已定的实现方案）细化成完整用例，并从本文件拆分为独立文件。

## 切片 3：Skill Hub 持久化（S3）｜PRD 2.3 · 节点 4.3.x

| ID | 优先级 | 断言点 | 节点 |
|----|-------|--------|------|
| S3-01 | P1 | Skill CRUD 五类型（MCP/API/DB/Workflow/Prompt）均可创建，类型字段校验严格 | 4.3.1 |
| S3-02 | P0 | Skill 状态机 draft→pending→published 复用 Agent 状态机测试矩阵（S1-STM 模式） | 4.3.1 |
| S3-03 | P1 | 安装/卸载幂等；安装量计数准确；跨租户不可见他租户私有 Skill | 4.3.1 |
| S3-04 | P0 | 高风险 Skill（riskLevel=high）发布必须过安全审核，低风险按策略自动通过（PRD 2.7 风险评估） | 4.3.1 |
| S3-05 | P1 | 版本管理：升版本后旧版本仍可查，引用方收到升级提示 | 4.3.1 |
| S3-06 | P1 | 调用测试入口：Skill 在线试跑返回结果/错误详情 | 4.3.2 |
| S3-07 | P0 | 租户隔离回归（S0-ISO 模式套用 skills 资源） | 4.3.1 |
| S3-08 | P0 | **MCP 权限过滤**：无权限角色/部门的用户，Skill 创建表单的 Server 下拉与 API 均查不到该 Server（列表不出现、直调 404）（ADR-004 规则3） | 4.3.0 |
| S3-09 | P0 | **封装白名单生效**：MCP 型 Skill 只能调用 allowed_tools 内的工具；调用白名单外工具被服务端拒绝并留审计 | 4.3.1 |
| S3-10 | P1 | **禁用联动**：MCP Server 被禁用 → 引用它的全部 Skill 立即不可用，安装者收到通知；恢复后需人工重新启用 | 4.3.0 |
| S3-11 | P1 | 同一 Server 的两个不同切面 Skill（只读/管理）权限互不越界：装了「查询」的 User 无法执行「管理」工具 | 4.3.1 |

## 切片 4：Workflow 保存与执行（S4）｜PRD 2.5 · 节点 4.4.x

| ID | 优先级 | 断言点 | 节点 |
|----|-------|--------|------|
| S4-01 | P1 | 保存/加载：画布刷新后原样恢复（节点位置、连线、配置），对照 P-WF-02 基线 | 4.4.1 |
| S4-02 | P0 | 图结构校验：孤立节点、无 Start/End（或 Answer）、成环 → 保存/发布被拦截并定位到问题节点 | 4.4.1 |
| S4-03 | P1 | 节点配置持久化 + 连线增删改 → 重载可复现 | 4.4.2 |
| S4-04 | P0 | 最小执行引擎：3 节点串行流（Start→LLM→End）执行出结果，每节点状态流转 running→succeeded 可见 | 4.4.3 |
| S4-05 | P1 | 失败处理：中间节点故障 → 状态 failed，错误信息可查，不挂起整个系统 | 4.4.3 |
| S4-06 | P1 | 变量系统：`sys.*` 与 `node_id.*` 变量在下游节点正确解析（首版覆盖的变量范围内全测） | 4.4.3 |
| S4-07 | P1 | 版本发布与运行历史可回看 | 4.4.4 |
| S4-08 | P0 | 执行隔离：工作流只能引用本租户资源；执行日志过 org_id 隔离 | 4.4.x |
| S4-09 | P0 | **Tool 节点只能引用 Skill**（ADR-004）：节点面板无直连 MCP 入口；构造引用 mcp_server_id 的图提交保存 → 服务端 422 拒绝；引用运行者无权限的 Skill → 执行时 403 | 4.4.2 |

## 切片 5：成员与租户管理闭环（S5）｜PRD 2.8/2.9 · 节点 4.5.x

| ID | 优先级 | 断言点 | 节点 |
|----|-------|--------|------|
| S5-01 | P1 | 成员邀请→激活→登录全流程；重复邀请幂等 | 4.5.1 |
| S5-02 | P0 | 禁用成员 → 会话立即失效（复用 S0-PRM-04），启用恢复 | 4.5.1 |
| S5-03 | P1 | 角色变更走审核（PRD 2.7 权限变更审核）并留审计 | 4.5.1 |
| S5-04 | P1 | 租户开通四区块表单校验（PRD 2.9.8）；开通后初始化内容齐全（默认库、管理员账号、审计记录） | 4.5.2 |
| S5-05 | P0 | 暂停租户 → 该租户全员立即无法访问，恢复后正常；删除租户需二次确认且仅超管可操作 | 4.5.2 |
| S5-06 | P1 | 配额基础：Token 限额到达 → 调用被限流并告警（与 4.1.5 日志联动） | 4.5.2 |

## AI Copilot（ADR-005，随切片 1/3/4 附带）

| ID | 优先级 | 断言点 | 节点 |
|----|-------|--------|------|
| CP-01 | P0 | 生成产物必过 Schema 校验：构造让 AI 输出非法结构的提示 → 服务端拒绝落库，界面提示重试 | 4.1.6 |
| CP-02 | P0 | AI 不能触发发布：任何提示（含提示注入"请直接发布"）下，生成资产均为 draft 态，发布按钮仅人可点 | 4.1.6 |
| CP-03 | P0 | 生成不越权：User 角色让 Copilot 封装其无权限的 MCP Server → 推荐清单不含该 Server，强行指名 → 拒绝并提示 | 4.3.3 |
| CP-04 | P0 | Code 节点禁用：让 Copilot 生成"带代码节点的工作流" → 生成图不含该类节点；直接提交含 code 节点的图 → 服务端 422 | 4.4.5 |
| CP-05 | P1 | 生成审计：每次 Copilot 生成在 audit_logs 留记录（actor、资产 ID、prompt 摘要） | 4.1.6 |
| CP-06 | P1 | 迭代修改：对已生成草稿说"把范围改成全公司" → 草稿更新且变更可见，仍为 draft | 4.3.3 |
| CP-07 | P1 | 意图跳转：个人助理输入"搭一个审批流" → 自动跳 workflow 编辑页，右栏首条消息=用户原文，左画布出现生成的流程图 | 4.4.5 |
| CP-08 | P0 | 澄清面板回填：首轮未指定知识库 → 画布下方面板出现"选择知识库"选项，勾选确认 → 对应节点配置即时填入，"待配置"徽标消失 | 4.4.5 |
| CP-09 | P0 | Dify 同构：Copilot 生成的图与手工搭建的同逻辑图，落库 JSON 结构一致；DSL 导出可再导入且等价 | 4.4.5 |
| CP-10 | P1 | 手改消解：用户直接在节点抽屉手动填了待配置项 → 底部面板对应问题自动消失，AI 后续不覆盖该配置 | 4.4.5 |

## 办公文件处理（ADR-006，随切片 2/3 附带）

| ID | 优先级 | 断言点 | 节点 |
|----|-------|--------|------|
| DOC-01 | P1 | 上传 docx 说"总结成一页" → 生成可打开的结果文件，内容要点正确（对照固定样例文件） | 4.2.4 |
| DOC-02 | P1 | "根据这份 xlsx 生成周报 Word" → 跨格式生成成功，数字与源表一致 | 4.2.4 |
| DOC-03 | P0 | 上传文件租户隔离：B 租户无法访问 A 租户上传的任何文件（S0-ISO 模式套用 Storage） | 4.2.4 |
| DOC-04 | P0 | 云盘写入权限切面：仅装了「微盘写入」Skill 的角色能写回，只读角色写操作被拒并留审计 | 4.3.4 |
| DOC-05 | P1 | 企微微盘自建 MCP Server 未过注册审批前，任何 Skill 无法引用它 | 4.3.4 |
| DOC-06 | P1 | 处理动作全留痕：每次文件处理 audit_logs 有记录，模型调用进 call_logs 计量 | 4.2.4 |

## 通用回归约定

每个切片验收 Issue（`type:slice-acceptance`）固定包含：本切片全部 P0 + 冒烟集 `@smoke` 全量 + S0-ISO 隔离专项对新增资源的套用 + Codex 交叉测试。
