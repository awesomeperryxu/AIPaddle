# TC-S0 · 切片 0：地基闭环用例（认证/租户/权限/数据层）

> 对应节点：ROADMAP 3.1-3.7 · 标签：`type:api` + `flag:tenant-data`
> 测试数据约定：租户 A（org_a：管理员 adminA、普通用户 userA）、租户 B（org_b：管理员 adminB）。三个测试账号在 seed 脚本中固定创建。

## 3.1 数据库与迁移（S0-DB）

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-DB-01 | P1 | 迁移可重放 | 空库执行全部 supabase migrations → 成功建出全部核心表，重复执行不报错 | L3/脚本 | 3.1 |
| S0-DB-02 | P1 | 核心表约束 | 尝试插入缺 `org_id` 的业务数据 → 被数据库约束拒绝 | L3/Vitest | 3.1 |
| S0-DB-03 | P2 | seed 数据 | 执行 seed 脚本 → 生成 A/B 两租户与测试账号，可登录 | L3/脚本 | 3.1 |

## 3.2 注册 / 登录 / 退出（S0-AUTH）

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-AUTH-01 | P0 | 注册成功 | 新邮箱注册 → 创建成功，收到确认邮件（或按配置直接激活），能登录 | L4/Playwright | 3.2 |
| S0-AUTH-02 | P1 | 重复注册 | 已存在邮箱再注册 → 明确报错，不泄露"该邮箱已注册"以外的信息 | L3/Vitest | 3.2 |
| S0-AUTH-03 | P0 | 登录/退出闭环 | 登录 → 进入 Dashboard；退出 → 回到登录页；再访问受保护页 → 被踢回登录页 | L4/Playwright | 3.2 |
| S0-AUTH-04 | P1 | 错误凭证 | 错误密码登录 → 提示失败，不崩溃；连续多次失败有限流/提示 | L3/Vitest | 3.2 |
| S0-AUTH-05 | P1 | 会话过期 | 令牌过期后调 API → 401，前端引导重新登录 | L3/Vitest | 3.2 |
| S0-AUTH-06 | P0 | 密钥不泄露 | 检查前端产物（build 输出）→ 不含 `SUPABASE_SERVICE_ROLE_KEY`；浏览器网络面板无直连 Supabase 域名的请求（ADR-001 约定） | L5/脚本+手动 | 3.2 |

## 3.3 租户上下文（S0-CTX）

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-CTX-01 | P0 | 请求携带租户 | adminA 调任意业务 API → 服务端解析出 user=adminA、org=org_a（日志可验证） | L3/Vitest | 3.3 |
| S0-CTX-02 | P0 | 无租户拒绝 | 构造无租户上下文的请求 → 被中间件拒绝，不落库 | L3/Vitest | 3.3 |
| S0-CTX-03 | P1 | 新建数据归属 | adminA 创建一条业务数据 → 落库记录的 `org_id` = org_a | L3/Vitest | 3.3 |

## 租户隔离专项（S0-ISO）——TESTING.md L5①，任何一条失败 = P0 停线

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-ISO-01 | P0 | API 枚举隔离 | 以 adminB 身份逐个调用**每一类**资源的列表/详情 API，传入 org_a 的资源 ID → 全部 404/403，响应体不含 org_a 任何字段 | L5/脚本 `pnpm test:isolation` | 3.3/3.7 |
| S0-ISO-02 | P0 | RLS 兜底 | 绕过应用层直连数据库（模拟应用层被绕过），以 org_b 会话查 org_a 数据 → RLS 拦截返回 0 行 | L5/SQL 脚本 | 3.3 |
| S0-ISO-03 | P0 | 越权写入 | adminB 尝试更新/删除 org_a 的资源 → 拒绝且数据无变化 | L5/脚本 | 3.3 |
| S0-ISO-04 | P0 | ID 猜测防护 | 顺序/构造 ID 访问不存在及他租户资源 → 一律 404，响应时间无显著差异（防枚举探测） | L5/脚本 | 3.3 |
| S0-ISO-05 | P0 | 前端隔离 | adminB 登录 UI → 任何页面不出现 org_a 数据（Playwright 断言页面内容） | L4/Playwright | 3.6 |

## 3.4 权限校验（S0-PRM）｜PRD 2.8 角色：Admin/Developer/User/Auditor

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-PRM-01 | P0 | 角色矩阵 | 按"角色×操作"矩阵逐格测试（Admin 全通；Developer 可建不可审；User 只用；Auditor 只读+审）→ 与矩阵完全一致 | L2/Vitest（矩阵驱动参数化用例） | 3.4 |
| S0-PRM-02 | P0 | API 级拒绝 | userA 直接调管理员 API（不经 UI）→ 403（证明不是只靠菜单隐藏） | L3/Vitest | 3.4 |
| S0-PRM-03 | P1 | 角色变更即时生效 | adminA 把 userA 升为 Developer → userA 无需重新登录即可（或按设计重新登录后）获得新权限；降级同理 | L3/Vitest | 3.4 |
| S0-PRM-04 | P1 | 禁用账户 | adminA 禁用 userA → userA 现有会话失效，无法再调任何 API | L3/Vitest | 3.4 |

## 3.5 数据层切换（S0-DAL）

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-DAL-01 | P1 | Dashboard 真实数据 | Dashboard 页删除 mock 引用后 → 统计数字与数据库实际记录数一致（插入一条测试数据，数字 +1） | L3/Vitest + L4 | 3.5 |
| S0-DAL-02 | P1 | 刷新持久 | 操作产生数据 → 刷新页面数据仍在（对照 P-WF-02 的原型基线） | L4/Playwright | 3.5 |
| S0-DAL-03 | P2 | mock 引用清零 | `grep mock-data` 该页面目录 → 无业务数据引用 | L1/脚本 | 3.5 |

## 3.6 线上部署（S0-DEP）

| ID | 优先级 | 用例 | 步骤 → 预期 | 层/方式 | 节点 |
|----|-------|------|------------|--------|------|
| S0-DEP-01 | P0 | 线上冒烟 | 部署后在线上地址执行 S0-AUTH-03 全流程 → 通过；健康检查接口 200 | L4/脚本 | 3.6/0.7 |
| S0-DEP-02 | P1 | 环境变量隔离 | 线上环境变量与本地不串（打错误配置应启动失败并有清晰报错） | L5/手动 | 3.6 |
