# _legacy —— 隔离区（勿与当前代码混用）

本目录存放**早期用 v0.dev / lovable 等工具生成的产物**，仅作历史归档/设计参考，
**不参与编译（已在 tsconfig 排除）、不被当前代码 import**。

- `lovable/` —— lovable 生成的设计文档与 demo 组件（早期原型）

当前真实代码在 `app/` `components/` `lib/`。企业管理页早期原型另见分支
`recovered/feature-add-project`。切勿把本目录内容当作当前实现。
